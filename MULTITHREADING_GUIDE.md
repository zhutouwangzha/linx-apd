# Linx-APD 多线程处理指南

## 概述

Linx-APD 现在支持多线程事件处理，可以显著提高系统调用事件的吞吐量和处理效率。多线程架构将事件获取和规则匹配分离到不同的线程池中，避免单线程瓶颈。

## 架构设计

### 线程模型
- **获取线程池 (Fetcher Pool)**: 负责从 eBPF ring buffer 获取事件
- **匹配线程池 (Matcher Pool)**: 负责事件富化和规则匹配

### 数据流
```
eBPF Ring Buffer → 用户态环形队列 → 获取线程 → 匹配线程 → 告警输出
```

## 启用多线程

### 1. 配置文件设置

在 `yaml_config/linx_apd_config/linx_apd.yaml` 中添加事件处理器配置：

```yaml
# 事件处理器配置
event_processor:
  enabled: true                    # 启用多线程处理
  fetcher_thread_count: 0          # 获取线程数，0表示使用CPU核心数
  matcher_thread_count: 0          # 匹配线程数，0表示使用CPU核心数*2
```

### 2. 线程数配置建议

- **获取线程数**: 建议设置为 CPU 核心数
- **匹配线程数**: 建议设置为 CPU 核心数的 1.5-2 倍
- **特殊场景**: 如果规则匹配计算密集，可以增加匹配线程数

### 3. 性能调优参数

```yaml
# 用户态环形队列大小 (在代码中通过宏定义)
LINX_EBPF_USER_RING_CAP: 16384    # 默认值，可根据需要调整

# eBPF 内核环形缓冲区大小
# 需要在 eBPF 加载时配置
```

## 使用示例

### 基本使用
```bash
# 使用默认配置启动
sudo ./build/bin/linx-apd -c yaml_config/linx_apd_config/linx_apd.yaml

# 使用自定义规则文件
sudo ./build/bin/linx-apd -c yaml_config/linx_apd_config/linx_apd.yaml -r yaml_config/linx_apd_rules/read_passwd.yaml
```

### 测试多线程功能
```bash
# 运行测试脚本
./test_multithreading.sh
```

## 性能优化建议

### 1. 系统级优化
- 使用 `isolcpus` 内核参数隔离 CPU 核心
- 调整 CPU 频率策略为 `performance`
- 禁用 CPU 节能特性

### 2. 应用级优化
- 根据系统负载调整线程数
- 监控事件丢失率，适当增加队列大小
- 优化规则匹配逻辑，减少字符串操作

### 3. 监控指标
- 事件处理延迟
- 规则匹配命中率
- CPU 使用率
- 内存使用情况

## 故障排除

### 常见问题

1. **编译错误**
   - 确保所有依赖库已安装
   - 检查编译器版本兼容性

2. **运行时错误**
   - 检查权限设置 (需要 root 权限)
   - 验证 eBPF 支持
   - 查看日志输出

3. **性能问题**
   - 调整线程数配置
   - 检查系统资源使用情况
   - 优化规则复杂度

### 调试技巧

1. **启用详细日志**
```yaml
log:
  level: DEBUG
```

2. **监控线程状态**
```bash
# 查看进程线程数
ps -eLf | grep linx-apd | wc -l

# 查看 CPU 使用情况
top -p $(pgrep linx-apd)
```

3. **性能分析**
```bash
# 使用 perf 分析热点
sudo perf record -g -p $(pgrep linx-apd)
sudo perf report
```

## 注意事项

### 线程安全
- 当前实现中，事件富化和规则匹配仍使用全局状态
- 多线程匹配时可能存在竞态条件
- 建议在高并发场景下进行充分测试

### 资源管理
- 合理设置线程数，避免过度并发
- 监控内存使用，防止内存泄漏
- 定期检查系统资源使用情况

### 兼容性
- 多线程功能向后兼容单线程模式
- 可以通过配置禁用多线程处理
- 支持动态配置重载

## 未来改进

1. **线程本地存储 (TLS)**
   - 将全局状态改为线程本地存储
   - 消除多线程竞态条件

2. **批量处理**
   - 支持事件批量富化和匹配
   - 减少线程切换开销

3. **自适应调优**
   - 根据系统负载自动调整线程数
   - 智能队列大小管理

4. **监控集成**
   - 集成 Prometheus 指标
   - 提供详细的性能监控

## 总结

多线程处理是 Linx-APD 性能优化的重要一步。通过合理的配置和调优，可以显著提高事件处理吞吐量，特别是在高频系统调用场景下。建议在生产环境部署前进行充分的性能测试和压力测试。