# linx_apd 实现概述与模块规划

## 一、实现概述（Current Implementation）

- 技术栈与依赖：C、eBPF、pthread、libyaml、PCRE2、uthash、cJSON、libbpf（内核采集），整体通过 `Makefile` 构建。
- 总体架构：
  - Kernel space：`kernel/ebpf` 或 `kernel/kmod` 采集事件，经由 ring buffer 上传。
  - Collection：`userspace/linx_engine` 抽象数据源，统一读取事件。
  - Processing：`userspace/linx_event_rich` 丰富上下文。
  - Rules：`userspace/linx_rule_engine` 解析并匹配规则。
  - Output：`userspace/linx_alert` 异步输出告警。
  - Control：`userspace/linx_apd` 作为入口，负责初始化、事件循环、信号与资源清理。
  - Utils：`linx_log`、`linx_thread`、`linx_hash_map`、`linx_yaml`、`linx_regex`、`linx_process_cache` 等。
- 当前能力亮点：
  - eBPF 事件采集（部分 syscall，`json_config/interesting_syscalls.json` 列表驱动）。
  - 规则引擎已具备词法/AST/匹配骨架，支持 and/or/not、比较与若干字符串操作符，支持输出模板编译与格式化（部分）。
  - 日志异步落地、进程缓存后台扫描、统一引擎抽象、stdout 告警通道、线程池化的异步任务执行。


## 二、模块规划（Per-Module Roadmap）

### 1. `userspace/linx_apd`
- 现状：主程序入口，顺序初始化各模块；注册 `SIGINT/SIGUSR1`；资源按逆序清理。
- 待实现：
  - 健壮的优雅退出（事件队列清空、规则匹配收敛、各线程优雅停止超时控制）。
  - 运行期健康探针与统计导出（/proc 或自定义端口）。
  - 配置热更新联动（与 `linx_config` 原子切换对齐）。

### 2. `userspace/linx_arg_parser`
- 现状：支持 `-c/--config` 与 `-r/--rules`、`-V/--version`。
- 待实现：
  - 扩展参数：日志级别/输出、引擎类型、线程数、丢弃策略、输出通道开关等。
  - 参数合法性校验、环境变量覆盖、默认配置落盘提示。

### 3. `userspace/linx_config`
- 现状：解析日志与 ebpf 相关配置；导出 `linx_global_config_t`；热更新未实现。
- 待实现：
  - 扩展 schema：完整 `engine.kmod`、`engine.plugin`、`linx_alert.*`、队列与并发配置等。
  - inotify 监控 + 原子配置切换（读写并发安全、双缓冲）。
  - 完整的配置校验与错误定位（字段级报错）。

### 4. `userspace/linx_log`
- 现状：多级别、可定向输出、异步写入。
- 待实现：
  - 日志滚动与压缩、结构化 JSON、上下文字段（pid/tid/rule）。
  - 背压与丢弃策略、与线程池/队列指标联动。
  - 可选 syslog 输出（或复用 `linx_alert.syslog`）。

### 5. `userspace/linx_hash_map`
- 现状：字段绑定与偏移映射，O(1) 查询；支持按表管理与批量绑定。
- 待实现：
  - 线程安全与读多写少优化、批量更新 API、内存上限与淘汰策略。
  - 绑定元信息导出（便于规则字段校验/提示）。

### 6. `userspace/linx_process_cache`
- 现状：后台扫描 `/proc`，缓存进程信息；极短生命周期进程可能漏采。
- 待实现：
  - 与 eBPF `execve/exit` 事件联动主动更新，缓解短进程遗漏。
  - 丰富字段：用户/组、cgroup/container、命名空间、capabilities。
  - LRU 过期与统计、fanotify/inotify 辅助监听。

### 7. `userspace/linx_event_rich`
- 现状：聚合 `evt.*` 字段，部分上下文丰富；结构体定义初步完成。
- 待实现：
  - 按 syscall 场景化丰富：文件元数据、网络 5 元组、用户/组、父子进程链路。
  - 统一 `evt.args` 与参数提取规范，失败码/方向/时间精度一致性。
  - 风险评级与标签化（可选）。

### 8. `userspace/linx_event_queue`（未实现）
- 待实现：
  - MPSC/MPMC 队列封装、批处理、零拷贝传递；可配置的 backpressure 与丢弃策略。
  - 与 `linx_engine`/`linx_event_processor` 的边界与吞吐指标。

### 9. `userspace/linx_event_processor`（未实现）
- 待实现：
  - 多线程流水线化处理：获取 -> 丰富 -> 匹配 -> 输出；任务切分与亲和（按 pid 或 FD）。
  - 平衡吞吐与有序性保证；规则集快照与并发匹配隔离。

### 10. `userspace/linx_alert`
- 现状：stdout 输出可用；线程池异步发送；消息格式化。
- 待实现：
  - file/http/syslog/executor 四种后端的完善（重试、限速、批量、幂等）。
  - 输出模板更丰富（结构化输出、字段缺省处理、escape/JSON）。
  - 通道开关与优先级路由、失败告警聚合与降噪。

### 11. `userspace/linx_rule_engine`
- 现状：lexer/AST/加载/匹配基础能力；已支持 and、or、not、>、>=、<、<=、=、!=、contains、icontains、startswith、endswith、in；输出模板编译与格式化（部分）。
- 待实现：
  - 支持 `val()` 与字段参数形式（如 `evt.arg.xxx` 动态参数）；完善 `match_context`。
  - 规则检查与更友好的错误信息；YAML 宏与 include；规则集的原子替换与热加载。
  - 匹配加速：预编译谓词、常量折叠、短路优化、PCRE2 JIT/缓存。
  - 字段覆盖面拓展：补齐 `evt.* / proc.* / user.* / group.* / fd.*`。

### 12. `userspace/linx_engine`
- 现状：抽象数据源；已实现 eBPF 分支；统一 `init/start/stop/next/close` 接口。
- 待实现：
  - `kmod` 分支实现与与 `ebpf` 事件结构对齐；`plugin` 框架（dlopen/vtable）。
  - 采集过滤：pid/comm 白名单、失败事件丢弃、drop 模式策略化。
  - ring buffer 读策略（批量/超时/唤醒）与指标。

### 13. `userspace/linx_regex`
- 现状：PCRE2 包装，提供基础匹配接口。
- 待实现：
  - 正则编译缓存、匹配超时与中断、JIT 与大小写/多行模式选项。

### 14. `userspace/linx_thread`
- 现状：线程池、任务提交/暂停/恢复/终止、状态查询。
- 待实现：
  - 优先级/配额、工作窃取、任务超时与取消、资源统计与可观测性。

### 15. `userspace/linx_yaml`
- 现状：加载与节点访问封装；基于 libyaml。
- 待实现：
  - 宏替换、列表/映射便捷 API、文件包含、严格 schema 校验与错误上下文。

### 16. `kernel/ebpf`
- 现状：脚本化生成表与 syscall id；示例 `open.c`；兴趣 syscall 列表已提供。
- 待实现：
  - 核心 syscall 覆盖：`execve/sendto/recvfrom/read/write/openat/dup*` 进入/退出钩子与参数采集。
  - CO-RE 兼容、BTF 校验、统一事件结构；ringbuf 批量上传；过滤策略下沉。
  - 网络维度映射到 `fd.*` 字段、错误码与方向一致性、丢弃策略与 backpressure。

### 17. `kernel/kmod`
- 现状：目录结构占位，未实现。
- 待实现：
  - kprobe/kretprobe 实现同等采集能力；与 eBPF 事件结构完全对齐，作为低内核版本回退方案。

### 18. `scripts`
- 现状：事件表/类型/syscall id 生成脚本。
- 待实现：
  - 生成流程集成构建；校验/去重/差异提示；与 `yaml_config` 联动。

### 19. `yaml_config` 与 `json_config`
- 现状：示例主配置与规则、兴趣 syscall 列表。
- 待实现：
  - 完整示例与注释，覆盖多通道输出、多引擎、过滤与并发配置；提供回归用规则集与小型数据集。


## 三、跨模块遗留问题（重点待解）
- 事件循环单线程与规则匹配串行，影响吞吐与延迟。
- 事件上下文丰富不够充分；短生命周期进程在缓存中缺失。
- 规则字段与 eBPF 参数抽取尚未完全对齐；`evt.args` 等字段未完全实现。
- 仅 stdout 输出；其余通道未落地。引擎仅 eBPF 分支可用。
- YAML 不支持宏替换与 include；规则不支持 `val()` 与参数化字段。


## 四、建议的近期优先级
- P0：实现 `execve` 富集联动与进程缓存修复；完成 stdout/file/syslog 三通道；`val()` 与字段参数化；事件处理并行化（基础）。
- P1：覆盖 `sendto/recvfrom/read/write/openat/dup*`；完善 `evt.args/fd.*`；日志滚动与结构化输出；配置热更新。
- P2：kmod 分支与插件框架雏形；规则集原子热更；正则 JIT/缓存；可观测性与统计面板。